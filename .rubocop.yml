inherit_from: .rubocop_todo.yml

require:
  - rubocop-performance
  - rubocop-rails
  - rubocop-rspec

AllCops:
  NewCops: enable

RSpec/Rails/InferredSpecType:
  Enabled: false
RSpec/DescribedClass:
  Enabled: false